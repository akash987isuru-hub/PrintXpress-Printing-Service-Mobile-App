<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/bg_home_screen"
    android:fitsSystemWindows="true"
    tools:context=".AdminReplyQueryActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="18dp"
        android:paddingEnd="18dp"
        android:paddingTop="28dp"
        android:paddingBottom="12dp">

        <TextView
            android:id="@+id/tvQueryDetails"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Support Chat"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="@color/px_primary"
            tools:ignore="HardcodedText" />
    </LinearLayout>

    <ScrollView
        android:id="@+id/adminChatScrollView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingStart="18dp"
        android:paddingEnd="18dp">

        <LinearLayout
            android:id="@+id/adminChatMessagesContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </ScrollView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="18dp">

        <EditText
            android:id="@+id/etAdminReply"
            android:layout_width="match_parent"
            android:layout_height="90dp"
            android:hint="Type admin message"
            android:gravity="top"
            android:padding="14dp"
            android:background="@drawable/bg_search_box"
            android:inputType="textMultiLine"
            android:autofillHints="none"
            tools:ignore="HardcodedText" />

        <Button
            android:id="@+id/btnSendReply"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Send Message"
            android:textAllCaps="false"
            android:textColor="@color/white"
            android:textStyle="bold"
            android:textSize="16sp"
            android:background="@drawable/bg_primary_button"
            android:elevation="3dp"
            android:layout_marginTop="12dp"
            tools:ignore="HardcodedText" />

        <Button
            android:id="@+id/btnBackSupportQueries"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Back to Chat Inbox"
            android:textAllCaps="false"
            android:textColor="@color/white"
            android:textStyle="bold"
            android:textSize="16sp"
            android:background="@drawable/bg_primary_button"
            android:elevation="3dp"
            android:layout_marginTop="10dp"
            android:layout_marginBottom="4dp"
            tools:ignore="HardcodedText" />
    </LinearLayout>
</LinearLayout>
